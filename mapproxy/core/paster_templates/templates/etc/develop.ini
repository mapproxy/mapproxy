[app:main]
use = egg:MapProxy#app
services_conf = %(here)s/services.yaml
proxy_conf = %(here)s/proxy.yaml
reload_files = %(here)s/*.*

[server:main]
## connect via tcp/ip
use = egg:Paste#http
host = 0.0.0.0
port = 8080
